#include <iostream>
using namespace;
void max_min(int *ptr, int size)
{
    int max = ptr[0];
    int min = ptr[0];
    for (int i = 0; i < size; i++)
    {
    }
}
int main()
{
    int size;
    cout << "Enter size of Array: ";
    cin >> size;

    int *ptr = new int[size];
    max_min(ptr, size);
}