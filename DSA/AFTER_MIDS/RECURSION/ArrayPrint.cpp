#include <iostream>
using namespace std;
void headPrint(int arr[], int size, int index = 0)
{
    // Base Case:
    if (index >= size)
    {
        return;
    }

    // Recursive Relation
}
int main()
{
    int arr[5] = {1, 2, 3, 4, 5};
    int size = 5;

    headPrint(arr, size);
}